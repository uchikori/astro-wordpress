---
type Props = {
  styles?: string;
  classes?: string;
  label?: string;
  url?: string;
};
const { styles, classes, label, url } = Astro.props;
---

<li class={classes} style={styles}>
  <a href={url}>
    {label}
  </a>
</li>
